name: Fuzz APWorld
on:
  push:
  pull_request:

jobs:
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: Eijebong/ap-actions/fuzz@main
        with:
          apworld-path: manual_xenobladechroniclesv2_xanderoni_skipperki1
          ap-version: 'latest'
          python-version: '3.12'
          runs: 50 # This is optional
          yamls-per-run: 1 # This is optional