name: Test APWorld
on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: Eijebong/ap-actions/ap-tests@main
        with:
          apworld-path: manual_xenobladechroniclesv2_xanderoni_skipperki1
          apworld-name: manual_xenobladechroniclesv2_xanderoni_skipperki1
          ap-version: 'latest'
          python-version: '3.12'
